import React from "react";
declare const BorderProgressBar: (props: {
    strokeWidth: number;
    strokeColor: string;
    progress: number;
    loading?: boolean;
}) => React.JSX.Element;
export default BorderProgressBar;
