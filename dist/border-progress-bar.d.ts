import React from "react";
declare const BorderProgressBar: (props: {
    strokeWidth: number;
    strokeColor: string;
    progress: number;
}) => React.JSX.Element;
export default BorderProgressBar;
